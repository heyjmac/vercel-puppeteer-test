{
  "name": "blank",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "https://github.com/heyjmac",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "serve": "node index.js",
    "start": "npm run build && node index.js",
    "dev": "concurrently -k \"npm --prefix client run build -- --watch\" \"nodemon --watch index.js --watch tools.js --ext js,mjs --exec 'node index.js'\""
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "connect-livereload": "^0.6.1",
    "livereload": "^0.10.3",
    "nodemon": "^3.1.10"
  }
}
